[Unit]
Description=Run the starter-public health probe every minute

[Timer]
# Wait a minute after boot so the app can bind its socket first.
OnBootSec=60s
OnUnitActiveSec=60s
AccuracySec=15s
Unit=starter-public-health.service

[Install]
WantedBy=timers.target
